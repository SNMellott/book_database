<% provide(:title, 'All books') %>
<h1>All Books</h1>

<%= link_to "Add a Book", newbook_path, class: "btn btn-info", role: "button" %>

<ul class="books">
  <% @books.each do |book| %>
    <%= render book %>
  <% end %>
</ul>
<%= will_paginate %>
